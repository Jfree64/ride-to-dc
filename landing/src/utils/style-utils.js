export const gradientStyleToRight = (gradientValues) => (
  `linear-gradient(to right, ${gradientValues.join(', ')})`
)

export const gradientStyleToBottom = (gradientValues) => (
  `linear-gradient(to bottom, ${gradientValues.join(', ')})`
)